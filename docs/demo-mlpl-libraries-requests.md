# Requests to `../demo-mlpl-libraries`

Changes this repository needs from the `demo-mlpl-libraries` library
collection. Nothing here is implemented from this repository; each entry is a
work order for that repository's agent, revalidated against the named
artifacts before it acts. Only requests raised by the active saga
(`reasoning-extensions-saga.md`) are listed.

| Id | Request | Raised by | Status |
|---|---|---|---|
| (none yet) | The saga plan was validated without finding a required library change. Steps append entries here when they hit one. | | |
