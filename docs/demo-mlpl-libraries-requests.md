# Requests to `../demo-mlpl-libraries`

Changes this repository needs from the `demo-mlpl-libraries` library
collection. Nothing here is implemented from this repository; each entry is a
work order for that repository's agent, revalidated against the named
artifacts before it acts.

No requests are open. The `reasoning-extensions` saga
(`reasoning-extensions-saga.md`, complete 2026-09-22) finished without
needing a library change: the tokenizer facade and download facade are
package-local `module.mlpl` files, and chat templating stays in the
consumer.
