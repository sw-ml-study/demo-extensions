Implement the bounded callback-free HTTP server described in docs/network-db-extensions.md using typed generational handles and focused Rust TDD. Add listen, bounded next-request polling, respond, and close behavior; apply fixed limits/CORS/token middleware ordering without invoking MLPL from native threads; use deterministic loopback tests; document security, backpressure, lifecycle, and upstream integration limits; run the full mandatory gate, commit, complete, and push main without sibling changes.