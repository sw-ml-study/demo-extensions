Using Rust TDD, define the smallest generic renderer-neutral point-scene contract for a bulk positions [N,3] array with parallel size, RGBA color, opacity, and stable non-negative ID arrays. Reuse existing dense-array validation and camera conventions; keep parsing/validation/upload planning separate from GPU, window, ABI, filesystem, and application semantics. Reject malformed shapes, nonfinite values, invalid colors/opacities/sizes, duplicate IDs, and explicit point-count/byte budget violations. Add deterministic headless planning evidence, update public contracts/docs and narrow gitignore rules if needed, run focused tests then just check, commit on main, complete the AgentRail step, and push. Do not add wgpu/winit rendering or an application demo in this step, and do not modify sibling repositories.