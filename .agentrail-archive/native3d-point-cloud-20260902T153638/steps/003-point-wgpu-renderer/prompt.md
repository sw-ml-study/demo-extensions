Using Rust TDD, integrate the generic bounded PointScene pipeline into the existing wgpu/winit native3d window renderer. Begin with focused failing tests for renderer-neutral GPU point vertex/instance planning and empty/bounded plans, then add the smallest responsibility-separated GPU data path that renders circular or clearly point-like sprites with per-point size/color/opacity and stable IDs retained for later picking. Reuse existing camera, viewport, lifecycle, and line-renderer infrastructure; keep unsafe/ABI boundaries unchanged and keep headless point tests authoritative. Document GPU buffer layout, copy/ownership semantics, limits, platform scope, opt-in interactive smoke instructions, and remaining picking/MLPL work. Do not add application semantics, a game engine, HTML/CSS/JavaScript/WASM, or modify sibling repositories. Run scoped Rust tests and strict Clippy, scripts/check-mlpl-style, and full just check; audit .gitignore/tracked files; commit before agentrail complete and push main.