Using test-driven development, add a deterministic self-explaining MLPL point-cloud demo that owns all application semantics and drives the delivered generic set_points, patch_points, set_view, and point_selection contracts. Start with native mlplunit tests for deterministic bounded point generation, parallel array shapes/IDs, selection state transitions from exact string IDs and no-hit events, camera interaction, revision monotonicity, patch construction, and teardown. Keep Rust limited to generic host plumbing; add only the smallest source-composition and CLI/just entry points needed to run the MLPL applet through the existing native wgpu/winit window. The initial teaching demo should explain which arrays are MLPL-owned, visibly react to stable-ID selection, use deterministic data without network/model downloads, and remain distinct from the later embedding/PCA application. Add headless Rust host tests proving the real MLPL worker emits valid complete/patch/view commands and consumes selection events while staying live. Document controls, MLPL-versus-Rust responsibilities, bounds, opt-in graphical smoke instructions, and limitations. Do not modify sibling repositories or introduce HTML/CSS/JavaScript/WASM. Run the focused mlplunit and Rust tests, canonical MLPL formatting/docstring checks, strict scoped Clippy, and full just check; audit .gitignore/tracked files; commit before agentrail complete and push main.