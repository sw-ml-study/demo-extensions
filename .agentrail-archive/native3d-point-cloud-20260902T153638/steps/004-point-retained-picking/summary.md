Delivered generic retained point-scene replacement and atomic patch commands, shared revision/view tracking, bounded operation and final-state validation, safe-integer command identities, point-only live rendering, and left-release selection events using the authoritative overlap policy. Hit/no-hit records preserve IDs and revisions as exact decimal strings over the existing owned MLPL record path, so no C ABI change was needed. Focused retained/picking tests, complete native-window tests, strict Clippy, MLPL style, and full just check passed. Committed as 36c16a0; the deterministic MLPL point-cloud application remains next.