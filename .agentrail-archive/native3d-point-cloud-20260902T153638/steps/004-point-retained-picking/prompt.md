Using Rust TDD, add generic retained point-scene updates and pointer-to-stable-ID selection delivery through the existing native3d window/input architecture. Begin with focused failing tests for atomic ID-addressed point upsert/remove transactions, revision mismatches, stale/unknown IDs, budget failures without partial mutation, and deterministic picking under overlap using the already authoritative PointRenderPlan tie policy. Add the smallest generic command/state types needed to carry complete point scenes and patches without domain semantics, then translate normalized physical-pixel pointer activation into an owned event containing the selected stable ID (or explicit no-hit) and revision. Reuse typed handles, bounded/coalesced input, camera/viewport planning, and lifecycle rules; keep MLPL responsible for application meaning and do not change the public C ABI unless an existing generic record path is insufficient and evidence is documented first. Keep headless tests authoritative; interactive checks are opt-in. Update docs, demos/catalogs affected by the generic contract, and explicit remaining scope. Do not modify sibling repositories or introduce HTML/CSS/JavaScript/WASM. Run focused Rust tests and strict Clippy, scripts/check-mlpl-style, and full just check; audit .gitignore/tracked files; commit before agentrail complete and push main.