Implement the MLPL-owned cube control reducer and application-state transitions against structured synthetic event records matching future bounded poll_events output. Cover width/height/length, signed speed, pause, reset, color cycle, thickness, resize, close, unknown events, bounds, and deterministic bulk scene updates with native mlplunit. Document the exact final event-loop seam and reconcile blockers/acceptance. Run all gates, commit, complete saga, metadata commit, push main, and stop.