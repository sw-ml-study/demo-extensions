Defined the MLPL-owned rotating wireframe cube contract and generic Rust line-scene interchange. Bulk MLPL array arithmetic produces eight dimension-scaled vertices and twelve edges; validated controls cover signed rotation speed, RGBA line color, and logical-pixel thickness. Rust rejects malformed schema/version, shapes, non-finite values, unsafe controls, and invalid indices, with canonical JSON for the temporary downstream bridge. Updated README, plan/demo order, saga catalog, layout policy, and detailed scene documentation. just check passed, including Cargo tests/clippy, upstream C-provider acceptance, mlplunit, gitignore, tracked-file, and repository policy checks. Native wgpu/winit rendering and direct upstream array/handle integration remain later steps.