Add the native wgpu/winit window using the proven renderer model. Render the rotating wireframe cube on macOS Metal and Linux-supported native wgpu backends. Keep an opt-in smoke command, no web/game engine. Add keyboard controls for width/height/length, rotation speed, color cycling, thickness, reset, pause, and close with visible help. Headless tests remain mandatory. Update docs/gates, commit, complete, push main.