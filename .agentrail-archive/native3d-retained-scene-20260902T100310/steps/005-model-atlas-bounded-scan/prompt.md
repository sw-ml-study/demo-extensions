Specify and implement the bounded model-file scan contract for Model Atlas without ever slurping a complete model or tensor payload into memory. Use file_size plus caller-capped read_bytes ranges, permit multiple deterministic passes, retain only budgeted catalog/summary IR, and fetch selected tensor detail on demand under explicit byte, iteration, tensor, rank, name, and cache bounds. Define Safetensors and GGUF adapter boundaries compatible with demo-ml-utils, prove sparse large-file peak memory is independent of total artifact size where feasible, reject malformed offsets/overlaps/overflow, document provenance and limitations, add mlplunit tests and fixtures, run full gates, commit, complete, and push main.