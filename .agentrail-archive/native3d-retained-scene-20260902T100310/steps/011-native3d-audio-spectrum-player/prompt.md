Build a bounded native MP3/Ogg spectrum-player demo that starts with a confined file picker, decodes incrementally without slurping the file, computes MLPL-owned stereo frequency bands, and renders a normal-speed interactive 3D equalizer with clearly labeled left/right, bass/mid/high ranges, play/pause, seek, orbit/pan/zoom, and optional synchronized audio. Reuse demo-file-processing contracts where possible; add only generic extension primitives, TDD with mlplunit and cargo test, document macOS/Linux behavior and measured timing/memory bounds, run just check, audit tracking/gitignore, commit, complete, and push main.