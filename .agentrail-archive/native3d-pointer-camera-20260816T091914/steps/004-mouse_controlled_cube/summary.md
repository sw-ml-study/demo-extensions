Migrated the live wireframe cube to the shared MLPL camera library. MLPL now owns left-drag orbit/tilt, wheel zoom, Shift-left and middle-drag pan alongside all keyboard controls; complete camera records cross with every scene command and the native overlay shows the bindings. Added mlplunit state-transition coverage and a Rust worker/Port test using normalized pointer events. Full just check passed and the real macOS winit/wgpu window was launched for opt-in smoke evidence.