Build an interactive native Conway's Life plane whose rules, deterministic presets, editing, animation, and camera gesture arbitration remain in MLPL while Rust provides only generic bounded input, native windowing, and bulk rendering. Deliver in four slices: pure finite-grid model and presets; editable controls and visible legend; live bounded animation with shared orbit/pan/zoom; then performance, teardown, macOS evidence, and Linux limitations. Every slice uses native mlplunit and applicable cargo TDD, updates docs and gitignore/tracking evidence, passes just check, commits, completes AgentRail metadata, and pushes main.