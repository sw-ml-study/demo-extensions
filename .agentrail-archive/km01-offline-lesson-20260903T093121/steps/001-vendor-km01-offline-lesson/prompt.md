Vendor microscope revision ff15ec7 and add KM01 to the offline lesson selector. Update provenance, checksums, fixture index, tests, and documentation. Confirm the existing generic rank/shape renderer handles [6,2] points and squared distances, repeated assignment/update observation names, alternating phase prefixes, and final zero centroid delta. Add no KMeansViewer or algorithm-specific Rust. Use TDD for executable behavior. Run focused Rust tests, the full repository gate, and sw-checklist. Treat live browser-to-server execution as a separate follow-up. Audit .gitignore and repository boundaries, commit source and AgentRail state before completion, complete the saga with --done, commit generated completion metadata, and push main.