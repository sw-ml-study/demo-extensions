Deliver the reusable demo-extensions graphics capabilities required by the SWTOS storage/runtime visualizer while preserving repository boundaries. sw-tos owns storage/runtime semantics, authoritative JSON emission, and its MLPL application; sw-mlpl owns host/runtime changes. This repository owns versioned bounded interchange validation fixtures, generic filled-box rendering, stable-ID picking/highlighting, camera/view modes, and downstream integration evidence. Start with static storage-layout interchange and headless proofs, then native interaction, then cross-repository fixture acceptance; do not add SWTOS-specific concepts to Rust.