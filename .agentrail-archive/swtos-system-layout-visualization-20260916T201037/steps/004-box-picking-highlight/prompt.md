Add generic stable-ID ray picking and selection highlighting for filled boxes, reusing the existing camera/input contracts. Selection must use deterministic nearest-hit/tie behavior; highlight is an orthogonal outline/accent and must not overwrite semantic color. Reject stale or missing IDs cleanly, keep input bounded, add headless interaction tests and an opt-in native smoke, document the event/command contract, run the mandatory gate, commit, complete, and push main.