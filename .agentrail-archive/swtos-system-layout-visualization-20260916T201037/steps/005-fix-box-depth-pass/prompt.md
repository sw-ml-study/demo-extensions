Reproduce and fix the native3d box-scene wgpu validation panic caused by mixing a Depth32Float box pipeline with depthless overlay pipelines in one render pass. Preserve box rendering and overlays, add focused regression coverage for the render-pass structure where practical, run the scoped Rust gates and full just check, and launch the real just box-scene-smoke window on macOS to confirm it remains alive without a validation panic before asking the user to retry.