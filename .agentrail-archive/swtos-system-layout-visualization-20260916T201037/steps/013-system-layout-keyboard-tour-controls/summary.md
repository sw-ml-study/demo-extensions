Fixed system-layout keyboard interaction end to end: arrows, brackets, and angle-bracket aliases now synchronize the BoxViewer selection with the GPU outline; WASD provides bounded orbit control; the overlay and docs expose the controls. The macOS tour targets the newest viewer and pins the requested prefix W W W A A A R H followed by 18 next-block selections before other mode/highlight commands, then stops rotation. Added application-boundary and exact tour-order regressions; full just check passed. No GUI actions were performed after the user requested uninterrupted desktop use.