Make the generic native box-scene viewer usable before authoritative OS fixture acceptance. Use TDD. Ensure --box-scene runs in a dedicated box-only mode with rotation off by default and an explicit toggle. Add bounded MLPL-owned presentation metadata for stable-ID selection annotations, a visible legend, and selectable/clickable color modes without encoding OS-specific semantics in Rust. Update the synthetic system-layout MLPL demo to supply region labels/details and color-by-kind/owner/space controls. Cover headless parsing/state/hit-testing plus MLPL behavior, run just check, document controls and ownership, commit, complete, and push main.