Added a generic one-shot initial camera override and fixed startup ordering so MLPL's initial scene command cannot reset it. The SWTOS layout now opens from above-left at yaw -0.70 and pitch 0.42 while preserving zoomed-out framing and normal subsequent orbit. Verified the exact new process visually, added focused regression coverage, and passed scoped Clippy plus full just check. Queued keyboard camera presets and visible block traversal as step 013.