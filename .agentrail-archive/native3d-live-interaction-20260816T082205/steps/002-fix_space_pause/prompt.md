Fix the live native 3D applet Space key so it reliably toggles pause/resume. Use TDD with a focused end-to-end Rust/MLPL event regression that proves Space makes rotation speed zero and a second Space restores the prior speed. Preserve MLPL ownership of control semantics and keep Rust limited to generic physical-key normalization and host primitives. Update affected help/controls/acceptance documentation, inspect .gitignore and tracked files, run focused cargo tests and native mlplunit plus the full just check gate, commit detailed implementation on main, complete the AgentRail step, commit durable metadata, and push origin main.