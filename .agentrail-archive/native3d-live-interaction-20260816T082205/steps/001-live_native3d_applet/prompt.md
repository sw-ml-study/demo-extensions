Using TDD, implement and launch the fully interactive native cube against adjacent sw-MLPL commits b2667843+ and e9930cfb+. The winit/wgpu host must own the main thread; MLPL runs on the worker through the public applet/Port contract; event polling is bounded; normalized key, resize, and close records feed demos/wireframe-cube/controls.mlpl; MLPL alone changes width, height, length, signed speed, pause/reset, color, and thickness and emits bulk arrays/render state; Rust remains generic. Make just cube-3d launch this path, add headless protocol tests plus native mlplunit, manually launch and verify the window, update docs/blockers/gitignore/layout, run just check, commit, agentrail complete --done, metadata commit, and push main. Do not modify ../sw-mlpl.