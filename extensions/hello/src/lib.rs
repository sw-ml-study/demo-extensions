//! Small external extension used to prove the public authoring path.
//!
//! Registration is deferred until the ABI and loader steps define it through
//! failing tests. This crate already builds as the independently loadable
//! artifact shape that those steps will exercise.
