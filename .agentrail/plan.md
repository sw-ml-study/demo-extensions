Deliver the native capabilities requested by ../reasoning-from-scratch in docs/demo-extensions-requests.md (E1 hftok Hugging Face byte-level BPE tokenizer extension, E2 bounded checksum-verified large-artifact download over http-client, E3 unpack_bf16 fallback only if the sw-mlpl bf16 dtype slips). Revalidation on 2026-09-16 found none implemented and the upstream E1 trigger artifacts not yet published, so the download ships first, hftok is built against a synthetic fixture owned here, the upstream parity step is gated, and the bf16 step is a decision. Values cross the boundary only as strings, integer arrays, records, packed bytes, and opaque handles; chat templating, prompts, and model semantics stay in MLPL upstream. Sibling-repository needs go to docs/sw-mlpl-requests.md and docs/demo-mlpl-libraries-requests.md, never implemented from here. Executable plan and per-step acceptance: docs/reasoning-extensions-saga.md.