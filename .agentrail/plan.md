Replace the JSON-only cube smoke path with an end-to-end native applet. Verify and consume the shipped sw-MLPL parked-main and bounded Port contract; keep winit/wgpu on the main thread, run MLPL on the worker, normalize bounded keyboard/resize/close records, let controls.mlpl own all cube state transitions, and send deterministic bulk scene/render commands to the generic native side. Prove headless protocol/reducer behavior, retain opt-in real-window smoke acceptance on macOS and Linux, update just cube-3d to launch the interactive path, reconcile blockers/docs, run all gates, commit, complete, and push main.