Deliver a native macOS/Linux bulk point-cloud teaching path through the same public extension architecture, with MLPL-owned application semantics and generic Rust primitives. First specify and test bounded renderer-neutral point arrays and upload planning. Then add headless point rendering, integrate the wgpu/winit live path with retained stable-ID updates and picking, add a deterministic MLPL demo, and publish portability/performance acceptance. Preserve typed handles, C ABI safety, bounded/coalesced input, no browser stack, TDD, direct-main commits, and explicit limitations; do not modify sibling repositories.