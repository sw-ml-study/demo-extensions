Replace the temporary JSON-only integration with the newly shipped sw-MLPL extension data boundary in dependency order. First synchronize the downstream ABI and safe SDK with structured records. Then prove this repository's real descriptor passes arrays, handles, and records through the actual adjacent sw-MLPL interpreter. Build a generic headless native3d provider with handle lifecycle and bulk scene updates, followed by an MLPL-owned control reducer over structured synthetic events. Keep real winit event delivery explicitly blocked until the upstream event-loop service lands; finish with evidence, blocker reconciliation, tests, commits, and direct pushes to main.