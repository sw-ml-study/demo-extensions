Implement the first bounded synchronous Rust HTTP-client extension through the repository's public ABI/SDK patterns, starting with focused failing tests. Define reusable MLPL-configurable middleware policy records and documented ordering for future client/server concerns including request limits, CORS, token extraction/verification, authentication, and authorization, while keeping application policy in MLPL and avoiding native-to-MLPL callbacks. Use deterministic loopback tests without public-network dependence; document capability, ownership, timeout, redirect, header, byte-body, error, security, and future server contracts. Update manifests/catalogs/just/checks as required, run the mandatory full gate, commit, complete, and push main. Do not modify sibling repositories.