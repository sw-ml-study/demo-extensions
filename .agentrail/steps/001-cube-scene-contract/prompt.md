Use TDD to add the deterministic wireframe cube scene/control contract. MLPL must own cube dimensions and array-derived vertices/edges or a deterministic interchange fixture; Rust may parse and validate generic scene data but must not encode demo semantics. Specify width/height/length, rotation speed, line color, line thickness, bounds, defaults, malformed input, and deterministic serialization. Use native mlplunit for MLPL and cargo test for Rust. Update docs, gitignore/tracking audits, just check, detailed commit, complete step, metadata commit, and push main.