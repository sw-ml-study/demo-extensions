Defined and tested the minimal extension ABI V1: stable C layouts and tags, ABI/size negotiation, bounded fail-closed descriptor validation, host-owned metadata copies, reserved/duplicate checks, and Rust panic containment. Documented unsafe pointer preconditions and current upstream integration limits; strengthened just check with compile-only and clippy gates. No ../sw-mlpl changes were needed.