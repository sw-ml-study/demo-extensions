Implement the confined SQLite extension described in docs/network-db-extensions.md with focused Rust TDD. Use typed generational handles for connections/statements/transactions or cursors as justified, parameterized SQL, explicit result/row/byte and busy-timeout bounds, sandbox-root confinement, deterministic cleanup, and no application semantics in Rust. Add an MLPL facade and acceptance documentation; run the full mandatory gate, commit, complete, and push main without sibling changes.