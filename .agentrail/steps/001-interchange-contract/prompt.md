Define and document the renderer-neutral versioned system-layout interchange consumed by demo-extensions, using docs/research.txt as design input and an explicitly synthetic neutral fixture rather than SWTOS-specific Rust semantics. Specify bounded spaces, regions, relationships, addresses/extents, purpose/owner/location/state attributes, block and sector hints, stable IDs, optional C24IMG composition metadata, validation order, malformed-input behavior, provenance fields, and compatibility rules. Coordinate by contract only: do not modify ../sw-tos or ../sw-mlpl. Add schema/fixtures and deterministic Rust validation tests, update docs and catalogs, run the mandatory gate, commit, complete, and push main.