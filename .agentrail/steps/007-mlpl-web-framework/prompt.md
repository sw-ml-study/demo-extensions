Build the small MLPL web framework described in docs/network-db-extensions.md over the accepted HTTP server and SQLite primitives. Start executable MLPL behavior with native mlplunit tests. Provide routing, named parameters, request validation, MLPL-owned authorization and middleware composition, JSON/forms, escaped HTML/templates, cookies/sessions as justified, and a deterministic experiment-dashboard CRUD example. Keep sockets, protocol mechanics, and database resources in generic Rust extensions. Run the full mandatory gate, commit, complete, and push main without sibling changes.