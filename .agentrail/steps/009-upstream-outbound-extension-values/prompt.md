With the user's explicit authorization to modify ../sw-mlpl, implement recursive outbound MLPL record and packed-byte marshaling through sw-MLPL's existing versioned C extension provider adapter. Use focused upstream Rust TDD for nested records, bytes, ownership, malformed values, and provider invocation. Do not change ABI layout unnecessarily. Update upstream documentation and its own durable task/saga mechanism if present, run its mandatory gates, commit and push ../sw-mlpl. Then update this repository's upstream-contract evidence only as required, run this repository's full gate, commit, complete, and push main.