Implement pure encode and decode in extensions/hftok with TDD: byte-to-unicode mapping, pre-tokenization using the pattern string read from the file via fancy-regex (lookahead support, no C onig build), merge-rank byte-level BPE, added-token and <|...|> plus think-tag control splitting that maps control tokens to their ids, and the inverse decode that keeps control tokens visible. Tests: every expected encoding on fixtures/tokenizer/tiny-tokenizer.json, byte round-trips on random inputs, unknown token errors, empty input, and malformed id arrays. Keep the module free of chat roles, prompts, and model semantics. Document the algorithm and its limits in docs/hftok-extension.md. Run scoped cargo test then just check, commit, complete, and push main.