Exposed generic filled boxes, perspective/orthographic view state, stable-ID picking/highlighting selection, rendering state, and lifecycle through the public native3d V1 C provider with bounded copied arrays and typed generational handles. Added the package manifest/helper module, dynamic/static parity tests, real interpreter coverage, and a stock mlpl-repl dylib acceptance gate. Documented that the cdylib is headless because the V1 ABI lacks main-thread event-loop/Port injection, and that native handles cannot yet bind to user-function facade parameters. Full just check passes.