Expose _hftok:load, encode, decode, token_to_id, and info through the SDK with typed generational handles, an extension.toml package manifest for the four supported targets, and a module.mlpl facade that keeps the requested one-argument public load(path) signature over sqlite-style root confinement (document the exact root resolution). Return ids as an integer dense array, decode as a string, token_to_id as an integer or err, and info as a record with vocabulary size, the end-of-text, turn-start, turn-end, and think ids, and the pattern string. Prove stale, wrong-type, and cross-extension handles fail closed and that every failure is an err result. Add loader contract tests, an mlplunit test through load_extension, and a just hftok-check recipe. Record any host gap in docs/sw-mlpl-requests.md and any library need in docs/demo-mlpl-libraries-requests.md. Run just check, update docs, commit, complete, and push main.