Close the native3d point-cloud saga with evidence-driven portability, performance, packaging, and documentation acceptance. Use test-first checks/scripts where executable behavior is added. Verify the deterministic MLPL demo and generic point path through native mlplunit, headless Rust tests, release builds, manifest/package boundaries, and the existing public extension architecture; add a bounded reproducible performance probe across representative point counts that reports CPU planning/vertex expansion bytes and timing without claiming GPU throughput from headless evidence. Confirm macOS behavior available in this environment and document Linux support as build/contract evidence unless an actual Linux runtime is available. Audit interactive smoke instructions, stable-ID selection/accessibility limitations, buffer copy/ownership language, no-network behavior, and that no sibling repository changed. Update README, docs/plan.md, docs/sagas.md, the point-cloud docs, and a dedicated acceptance matrix with exact evidence and limitations. Do not add embedding/PCA semantics, downloads, HTML/CSS/JavaScript/WASM, a game engine, or modify sibling repositories. Run focused checks, scripts/check-mlpl-style, strict scoped Clippy, release build, and full just check; audit .gitignore/tracked files; commit before agentrail complete, complete with --done if all acceptance criteria pass, and push main.