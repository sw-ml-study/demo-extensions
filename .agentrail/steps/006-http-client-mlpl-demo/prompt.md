Add an honest executable .mlpl HTTP-client example and real MLPL-to-provider acceptance path after the callback-free server foundation. Use a deterministic loopback endpoint for mandatory tests and an opt-in bounded HTTPS time example against https://time.gov/ for humans; remote availability must not gate acceptance. Do not fake native invocation through an identity facade: either use an existing public sw-MLPL static-provider host hook from this repository or document the exact upstream blocker and add the closest runnable host command without modifying siblings. Update docs and checks, run the mandatory full gate, commit, complete, and push main.