Implement the explicit pinned model-acquisition layer described in docs/network-db-extensions.md only after the HTTP/server/filesystem contracts are proven. Use a short allowlisted model name, disclosed expected size, pinned URL and SHA-256, bounded streaming into a temporary file under an explicitly granted cache root, checksum verification, atomic rename, cache reuse, cancellation, and deterministic cleanup. Do not add arbitrary URL download or shell execution. Use local deterministic fixtures for tests; run the full mandatory gate, commit, complete, and push main without sibling changes.