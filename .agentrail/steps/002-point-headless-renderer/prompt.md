Using Rust TDD, add pure camera-space projection, viewport clipping/culling, depth ordering, and deterministic CPU raster evidence for the generic PointScene contract. Preserve stable IDs through planning and define overlap/picking tie behavior without adding application semantics. Keep point planning/rasterization separate from line logic where responsibilities differ, reuse generic camera math where appropriate, enforce bounded output/allocation behavior, update docs, run focused tests and just check, commit, complete, and push. Do not add wgpu/winit integration, ABI changes, or an MLPL application demo in this step, and do not modify sibling repositories.