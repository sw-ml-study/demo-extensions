Expose SHA-256 as a reusable native primitive so consumers can pin and verify artifacts without implementing the algorithm in MLPL, where 64 rounds of 32-bit modular arithmetic per 64-byte block over gigabyte inputs is infeasible (no native u32 type). Build a small responsibility-scoped extensions/digest crate, not an addition to http-client: _digest:sha256_bytes(bytes) -> lowercase hex string for in-memory data, and _digest:sha256_file(record{root, path, chunk_bytes}) -> record{sha256, bytes} that streams an existing confined file in bounded chunks without loading it, reusing the sqlite/download root-confinement rules (absolute canonical root, confined relative path, fail closed on traversal and escapes). Add a module.mlpl facade with docstrings, package manifest, loader contract test across dynamic and static providers, mlplunit coverage, deterministic Rust tests including the empty input, NIST 'abc' vector, a multi-block input, a chunk-boundary case, and missing/unreadable/directory paths as err results never panics. Refactor the download path to share one hashing implementation rather than duplicating it. Document the surface, limits, and the reason this is native in docs/digest-extension.md, and record in docs/reasoning-extensions-saga.md that this unblocks downstream artifact pinning. Do not modify any sibling repository; document needs for other repos in their respective docs/<repo>-requests.md here. Run the focused tests and the full gate, commit, complete, and push main.