Delivered the hftok contract, file validation, and fixture, starting work order E1 (upstream RS11). Wrote docs/hftok-extension.md and scaffolded extensions/hftok with pure tokenizer.json parsing and validation. Grounded the contract in two real tokenizer.json files found locally rather than the work order's prose, which corrected two assumptions that would have rejected every real vocabulary: the pre-tokenizer is a Sequence wrapping Split and ByteLevel rather than a bare ByteLevel, and merges are two-element arrays rather than space-separated strings; both shapes are now accepted and tested. That reading also confirmed the pre-tokenization pattern uses a negative lookahead the regex crate cannot compile, so step 005 needs fancy-regex as planned. Accepted files are BPE with ByteLevel decoder and byte-level pre-tokenizer; unsupported model types, decoders, pre-tokenizers, normalizers, missing sections, and malformed JSON are err values with reasons and nothing panics; absent control tokens report -1 rather than changing the record shape. Deviated deliberately and visibly from the work order on root confinement: it asks for load(path) against a configured sandbox root, but this repository has no ambient root and inventing one would grant the implicit authority the extension rules forbid, so the private surface takes an explicit root like the sqlite/download/digest surfaces and the facade will preserve the one-argument spelling in step 006. Published fixtures/tokenizer/tiny-tokenizer.json (synthetic but genuine: 256-entry byte-level alphabet, a merge list that actually applies, the real pattern, five control tokens) and tiny-expected-encodings.json derived from a byte-level BPE reference rather than hand-written, with the honest caveat that the reference is also mine so this is not yet independent confirmation; step 007 reconciles against the downstream MLPL reference and real Qwen3 goldens. 9 crate tests pass; full gate passed with PASS pre-commit after fixing a pedantic clippy lint the gate caught in the test.