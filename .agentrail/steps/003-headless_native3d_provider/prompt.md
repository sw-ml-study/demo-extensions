Build a generic headless native3d provider through the public ABI/SDK using TDD. Expose create_viewer returning a typed generational handle, bulk set_lines arrays, viewer state/size records, explicit render state, and deterministic close/stale rejection without opening a window or embedding cube semantics. Prove lifecycle, array validation, malformed calls, deactivation, and interpreter dispatch. Update docs and all gates; commit, complete, push main.