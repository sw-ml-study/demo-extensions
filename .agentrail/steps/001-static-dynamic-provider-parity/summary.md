Unified dynamic and static providers behind one descriptor validation and registration path. Dynamic guards retain libloading libraries; static providers use process-lifetime entry functions. Both modes now pass the identical hello success, failure, contained panic, namespace, arity, lifecycle, and deactivation contract. Archived the completed foundation saga and initialized the SDK/arrays/handles saga; no upstream files changed.