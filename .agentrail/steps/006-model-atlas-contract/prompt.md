Define and implement the bounded versioned Model Atlas interchange contract consumed by this repository, using checked-in deterministic Safetensors/GGUF-derived fixtures compatible with demo-ml-utils tensor-city IR. Keep parsing/classification/application semantics in MLPL; document cross-repository ownership and provenance; add native mlplunit schema, budget, malformed-input, and deterministic-layout tests; update docs and gates; commit, complete, and push main.