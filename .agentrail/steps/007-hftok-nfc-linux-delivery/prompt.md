Deliver the user-requested Linux-built hftok with Qwen3 NFC normalizer support and Linux checksum-verified download artifacts. Use focused failing Rust tests before implementing NFC normalization, preserving null-normalizer behavior and rejecting unsupported normalizers. Test normalization ordering around added tokens and normalized decode semantics; do not promise byte-exact round trips for decomposed input. Build hftok and http-client cdylibs on Linux and exercise public loader/MLPL surfaces. Document exact artifacts and integration commands for reasoning-from-scratch without modifying sibling repositories. Run scoped tests and just check, update contracts and acceptance evidence, commit named files on main, complete, commit completion metadata, and push origin main.