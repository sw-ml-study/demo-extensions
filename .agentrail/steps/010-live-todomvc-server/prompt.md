Wire the existing MLPL TodoMVC framework to the accepted callback-free HTTP server and confined SQLite providers now that outbound records/bytes are supported. Add just todomvc-server that binds loopback, prints its URL, persists CRUD state, and supports browser add/view/edit/toggle/delete/filter/clear-completed behavior. Add deterministic browser-to-server acceptance without external network, update docs, run the full mandatory gate, commit, complete, and push main without domain logic in Rust.