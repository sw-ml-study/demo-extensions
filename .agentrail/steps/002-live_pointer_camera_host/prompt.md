Using TDD, connect generic winit cursor, mouse-button, drag, wheel, and bounded/coalesced frame events to the existing Port, and accept MLPL-owned camera commands in the wgpu renderer. Keep platform translation and validation in Rust but no cube mappings. Cover headless event ordering/coalescing, command validation, resize interaction, shutdown, and macOS/Linux portability; update docs, run all gates, commit, complete, and push.