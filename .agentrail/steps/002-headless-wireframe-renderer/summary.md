Added a renderer-independent headless line pipeline consuming only generic scene arrays. It validates cameras and rotation, transforms around Y, near-clips, projects with aspect-correct perspective, viewport-clips, plans thick RGBA lines, and produces deterministic bounded RGBA/PPM evidence with a stable fingerprint. Tests cover rotation, aspect ratio, dimensions, style, degenerate geometry, clipping, bounds, invalid inputs, and reproducibility. Documented the first-pass PoC scope and the required generic extension API: typed window handles, bounded event polling, bulk scene updates, explicit render/present and timing, resize, and close. MLPL owns the application loop and all cube-specific control responses; Rust owns only native primitives. just check passed.