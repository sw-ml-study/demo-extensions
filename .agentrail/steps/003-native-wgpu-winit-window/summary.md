Added a real opt-in wgpu/winit desktop viewer for generic MLPL-generated line scenes. The shared native crate expands planned thick lines to GPU triangles, uses one WGSL pipeline, queries the surface format, handles resize/surface recovery, and exits via Escape/native close. just cube-3d runs sw-MLPL to create the deterministic scene then opens the rotating native window; the macOS smoke run succeeded. The same source uses winit Cocoa plus wgpu Metal on macOS and winit Wayland/X11 plus supported native wgpu backends on Linux; Linux was not smoke-run from this Mac. Following the user's clarified architecture, Rust does not implement cube-specific width/height/length/speed/color/thickness controls. Those remain MLPL-owned and are honestly blocked on upstream persistent handles, bounded event polling, bulk updates, event-loop integration, and compiler parity. Documented the PoC, platform handling, command spelling, and blocker. just check passed.