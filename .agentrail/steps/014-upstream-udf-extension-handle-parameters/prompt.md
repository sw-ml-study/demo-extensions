After separate explicit authorization to modify ../sw-mlpl, allow typed extension handles to bind to and pass through ordinary MLPL u: function parameters, preserving stale/wrong-type/cross-extension rejection. Add focused interpreter and provider regressions, update docs/upstream-contract.md, and follow both repositories' AgentRail and publication gates. Also investigate why freshly rebuilt debug/release mlpl-repl --version reports commits older than upstream HEAD.