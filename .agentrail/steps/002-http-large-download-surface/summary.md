Published the bounded large-artifact download through the public MLPL surface, completing work order E2. Registered _http:download(record) through the SDK; added the u:http_download facade and u:http_download_request record builder in module.mlpl; added demos/http-client/download.mlpl, an upstream-host binary, scripts/fetch-artifact, and a just fetch-artifact recipe whose root defaults to the newly ignored models/ directory. Documented the record, limits, error taxonomy, ownership split, and opt-in Qwen3 smoke procedure in docs/network-db-extensions.md; the Qwen3 sizes are published in the work order but their digests are not, so the recipe takes the digest from the publisher rather than vendoring an invented one. Loader contract test pins the registered name, help text, and argument rejection across dynamic and static providers; the mlplunit test pins the facade record shape without network I/O; no gate step invokes fetch-artifact. Verified end to end against a local server: a 200,000-byte payload published with its expected digest (reused: 0), a second run reported reused: 1 without opening a connection, and a tampered digest, a size mismatch, and a ../ path each failed with a distinct message leaving zero files beneath the root. Filed two host findings in docs/sw-mlpl-requests.md rather than working around them silently: R1, extension calls return a bare value on success but a result value on failure so neither is_ok nor field access is total across both (demo classifies with type_of and str_eq); R2, get_error fails on a string payload where err_message works. Neither blocks the saga. Full gate passed with PASS pre-commit. Committed 28ef340 on main.