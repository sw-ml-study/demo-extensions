Expose the bounded download built in step 1 through the public extension surface: register _http:download(record) via the SDK in extensions/http-client, add the u:http_download facade in module.mlpl with a docstring, add a loader contract test and an mlplunit test (loopback or fixture-based, never public network), document the record, limits, error taxonomy, and ownership split in docs/network-db-extensions.md, add a thin just fetch-artifact recipe backed by a script under scripts/, and record an opt-in real-network smoke procedure for Qwen/Qwen3-0.6B-Base tokenizer.json (7,031,645 bytes) and model.safetensors (1,192,135,096 bytes) writing under an ignored models/ root with .gitignore reviewed. Acceptance: a checksum-verified file appears with the published size and a truncated or tampered transfer leaves no partial file. If the host cannot marshal something the record needs, add the request to docs/sw-mlpl-requests.md rather than working around it silently. Run just check, commit, complete, and push main.