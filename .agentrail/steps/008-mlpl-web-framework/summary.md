Built the MLPL web framework over the accepted web/SQLite contracts: method-aware routes with named parameters, native-envelope validation, ordered MLPL middleware and authorization, JSON/forms, escaped HTML, secure cookies and parameterized session plans. Added standard TodoMVC add/edit/toggle/delete/filter/clear behavior, controller routes, escaped server-rendered view, SQLite CRUD plans, and an executable deterministic preview, plus experiment-dashboard CRUD plans. Documented that live provider composition remains gated by upstream outbound record/bytes marshaling. Full just check and just todomvc passed.