Delivered the native wgpu/winit point pipeline: deterministic planned points expand into circular alpha-blended GPU sprites with a tested 40-byte vertex layout, full split stable IDs, empty-plan suppression, bounded JSON loading, and an opt-in macOS/Linux smoke fixture. Existing camera, line, lifecycle, input, MLPL, and ABI boundaries remain unchanged. Scoped native-window tests, strict Clippy, MLPL style, and full just check passed. Committed as 9e27d08; retained point updates and pointer-to-ID delivery remain next.