Built and tested the independent hello cdylib and safe dynamic registry. The loader validates and copies metadata, creates namespaced callables, retains library lifetime, copies typed integer results and errors, contains extension panics before C return, and rejects calls after deactivation. Documented the concrete upstream blocker: sw-MLPL has no public registry/import hook, so this proves the downstream host harness but not .mlpl invocation.