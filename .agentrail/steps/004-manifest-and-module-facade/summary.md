Added deterministic TOML extension-package resolution, exact platform selection, ABI and namespace validation, canonical path confinement, duplicate detection, stable diagnostics, and resolved-package loading. Packaged hello with a public MLPL facade and private _hello native namespace, added Rust negative tests and native mlplunit coverage, and documented packaging. The existing sw-MLPL host-import blocker remains recorded; no upstream files changed.