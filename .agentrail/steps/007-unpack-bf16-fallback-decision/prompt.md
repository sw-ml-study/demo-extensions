Decision step for E3. Re-check ../sw-mlpl for bf16 support in reinterpret (request R7 from ../reasoning-from-scratch; see its docs/sw-mlpl-blockers.md) and record the evidence. If the dtype has landed, document not needed in docs/reasoning-extensions-saga.md and complete without code. Otherwise add a minimal extensions/bf16 crate exposing unpack_bf16(bytes) -> f32 dense array with tests for 1, -1, 2, 0.5, 0, 50, subnormals, positive and negative infinity, NaN, odd byte counts, and parity with the upstream bf16-vectorized-decode probe values; package manifest, facade, loader test, mlplunit test, and docs. No model semantics in Rust. Run scoped cargo test then just check, commit, complete, and push main.