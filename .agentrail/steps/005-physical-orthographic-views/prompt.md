Add reusable perspective/orthographic camera modes and a 2D physical-layout view over the exact same box scene and stable IDs. Preserve rotate/pan/zoom where meaningful, deterministic projection and picking, and renderer neutrality. Do not introduce storage-specific layout policy. Add focused headless tests, expose only generic commands/APIs needed by an MLPL application, update docs, run the mandatory gate, commit, complete, and push main.