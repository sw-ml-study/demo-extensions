After sw-tos defines its runtime snapshot/event artifact and sw-mlpl supplies any required host support, add bounded generic snapshot/event ingestion for allocation/load/exit/reclaim transitions. Animate or step stored extent to runtime boxes using MLPL-owned reducers while Rust remains a generic renderer. Preserve event order, stable identities, cancellation, backpressure, and deterministic replay; provide offline fixtures first and make live linkage optional. Run focused tests and the mandatory gate, document limitations/provenance, commit, complete, and push main.