Integrate generic filled boxes into the native winit/wgpu window path with reusable GPU vertices and retained stable IDs. Render the accepted neutral system-layout fixture, support resize and deterministic selection visibility, and keep the interactive smoke opt-in. No SWTOS-specific colors, labels, addresses, or load semantics in Rust. Add focused renderer/window/headless tests, update demo entry points and docs, run the mandatory gate, commit, complete, and push main.