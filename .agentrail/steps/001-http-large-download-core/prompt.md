Add a pure, testable bounded-download module to extensions/http-client following the design in docs/network-db-extensions.md and docs/reasoning-extensions-saga.md step 1. Define a request record with url, expected_bytes, sha256 (64 hex chars), absolute canonical root, confined relative path, chunk_bytes, and timeout_ms. Validation fails closed on non-canonical or missing roots, absolute or traversing paths, zero or over-cap expected lengths, malformed digests, and non-HTTP(S) URLs. Stream bounded chunks from the ureq Read body into a uniquely named temporary file under the root while hashing with sha2; verify the byte count against expected_bytes and Content-Length and the digest against sha256; atomically rename into place; remove the temporary file on every failure path. Reuse an existing target only when its size and checksum verify. Start with failing Rust tests over loopback listeners: success, truncated body, tampered bytes, over-declared length, Content-Length mismatch, redirect limit, timeout, and reuse. Keep pure validation/planning separate from filesystem and network effects; no application semantics in Rust. Run the scoped cargo test then just check, update docs, commit, complete, and push main.