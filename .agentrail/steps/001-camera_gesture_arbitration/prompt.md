Using native mlplunit and Rust worker/Port TDD, add MLPL-owned left-drag orbit/tilt, Shift-left or middle-drag pan, and wheel zoom to tic-tac-toe. Implement deterministic movement-threshold arbitration so a stationary left press/release places a mark while any camera drag suppresses placement; preserve hover, terminal safety, choices, restart, and close. Update visible help and docs, launch the corrected macOS window, run all gates, commit, complete, and push main.