After the ../../softwarewrighter/MesaOS agent publishes an authoritative MesaOS system-layout artifact conforming to the shared versioned interchange, pin its producer revision and checksum, validate it through the existing generic bounded parser, and prove the same MLPL box/view adapter and renderer consume it without MesaOS-specific Rust. Keep MesaOS semantics in producer data, document provenance and limitations, run the mandatory gate, commit, complete, and push main.