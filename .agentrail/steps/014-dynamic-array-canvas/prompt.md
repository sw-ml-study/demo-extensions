Implement the smallest honest graphical demo that follows TodoMVC's dynamic-loading path: a normal stock mlpl-repl process runs tracked MLPL source, load_extension opens a separately built Rust cdylib by explicit absolute path, MLPL computes deterministic bulk geometry and colors, and one generic blocking canvas call owns a native winit/wgpu window until close. Add a focused external-style canvas extension using the public ABI/SDK, no callbacks, no Port injection, no application-specific Rust semantics, macOS/Linux support, headless contract tests plus an opt-in interactive demo, a thin just/script launcher, documentation, and explicit extension-free failure evidence. Use TDD, run the full mandatory gate, commit, complete, and push main without sibling changes.