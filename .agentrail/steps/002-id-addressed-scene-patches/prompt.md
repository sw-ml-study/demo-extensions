Design and implement the generic retained-scene shadow-DOM slice: MLPL sends versioned ID-addressed line/object add/update/remove patches with base and target revisions, Rust validates and applies patches atomically to retained geometry, stale/malformed patches fail closed, and Life sends changed-cell patches rather than full scenes. Cover ownership, bounds, resync, performance, teardown, docs, macOS/Linux portability, full gates, commit, completion, push, and live verification.